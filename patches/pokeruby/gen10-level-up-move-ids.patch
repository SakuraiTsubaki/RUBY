diff --git a/include/pokemon.h b/include/pokemon.h
--- a/include/pokemon.h
+++ b/include/pokemon.h
@@ -1,8 +1,9 @@
 #ifndef GUARD_POKEMON_H
 #define GUARD_POKEMON_H
 
-#include "constants/pokemon.h"
-#include "sprite.h"
+#include "constants/pokemon.h"
+#include "ruby_gen10_ids.h"
+#include "sprite.h"
 
 enum {
     EGG_GROUP_NONE,
@@ -334,10 +335,15 @@
     u16 image[16];
 };
 
-struct __attribute__((packed)) LevelUpMove {
-    u16 move:9;
-    u16 level:7;
-};
+struct LevelUpMove
+{
+    RubyMoveId move;
+    u16 level;
+};
+
+#define LEVEL_UP_LEARNSET_MOVE(table, index) ((table)[(index) * 2])
+#define LEVEL_UP_LEARNSET_LEVEL(table, index) ((table)[(index) * 2 + 1])
+#define LEVEL_UP_LEARNSET_END 0xFFFF
 
 enum {
     GROWTH_MEDIUM_FAST,
diff --git a/src/data/pokemon/level_up_learnsets.h b/src/data/pokemon/level_up_learnsets.h
--- a/src/data/pokemon/level_up_learnsets.h
+++ b/src/data/pokemon/level_up_learnsets.h
@@ -5,8 +5,8 @@
 #ifndef POKERUBY_LEVEL_UP_LEARNSETS_H
 #define POKERUBY_LEVEL_UP_LEARNSETS_H
 
-#define LEVEL_UP_MOVE(lvl, move) ((lvl << 9) | move)
-#define LEVEL_UP_END 0xffff
+#define LEVEL_UP_MOVE(lvl, move) (move), (lvl)
+#define LEVEL_UP_END 0xffff, 0xffff
 
 const u16 gBulbasaurLevelUpLearnset[] = {
     LEVEL_UP_MOVE( 1, MOVE_TACKLE),
diff --git a/include/macros/pokemon_data.inc b/include/macros/pokemon_data.inc
--- a/include/macros/pokemon_data.inc
+++ b/include/macros/pokemon_data.inc
@@ -24,9 +24,10 @@
 	.2byte (\sp_defense << 10) | (\sp_attack << 8) | (\speed << 6) | (\defense << 4) | (\attack << 2) | \hp
 	.endm
 
-	.macro level_up_move level, move
-	.2byte (\level << 9) | \move
-	.endm
+	.macro level_up_move level, move
+	.2byte \move
+	.2byte \level
+	.endm
 
 	.macro evo_entry method, parameter, target_species
 	.2byte \method
diff --git a/src/pokemon_1.c b/src/pokemon_1.c
--- a/src/pokemon_1.c
+++ b/src/pokemon_1.c
@@ -1918,21 +1918,21 @@
     s32 level = GetLevelFromBoxMonExp(boxMon);
     s32 i;
 
-    for (i = 0; gLevelUpLearnsets[species][i] != (u16)-1; i++)
-    {
-        u16 moveLevel;
-        u16 move;
-
-        moveLevel = (gLevelUpLearnsets[species][i] & 0xFE00);
-
-        if (moveLevel > (level << 9))
-            break;
-
-        move = (gLevelUpLearnsets[species][i] & 0x1FF);
-
-        if (GiveMoveToBoxMon(boxMon, move) == (u16)-1)
-            DeleteFirstMoveAndGiveMoveToBoxMon(boxMon, move);
-    }
+    for (i = 0; LEVEL_UP_LEARNSET_MOVE(gLevelUpLearnsets[species], i) != LEVEL_UP_LEARNSET_END; i++)
+    {
+        u16 moveLevel;
+        RubyMoveId move;
+
+        moveLevel = LEVEL_UP_LEARNSET_LEVEL(gLevelUpLearnsets[species], i);
+
+        if (moveLevel > level)
+            break;
+
+        move = LEVEL_UP_LEARNSET_MOVE(gLevelUpLearnsets[species], i);
+
+        if (GiveMoveToBoxMon(boxMon, move) == (u16)-1)
+            DeleteFirstMoveAndGiveMoveToBoxMon(boxMon, move);
+    }
 }
 
 u16 MonTryLearningNewMove(struct Pokemon *mon, bool8 firstMove)
@@ -1945,24 +1945,26 @@
     // the game needs to know whether you decided to
     // learn it or keep the old set to avoid asking
     // you to learn the same move over and over again
-    if (firstMove)
-    {
-        sLearningMoveTableID = 0;
-
-        while ((gLevelUpLearnsets[species][sLearningMoveTableID] & 0xFE00) != (level << 9))
-        {
-            sLearningMoveTableID++;
-            if (gLevelUpLearnsets[species][sLearningMoveTableID] == 0xFFFF)
-                return 0;
-        }
-    }
-
-    if ((gLevelUpLearnsets[species][sLearningMoveTableID] & 0xFE00) == (level << 9))
-    {
-        gMoveToLearn = (gLevelUpLearnsets[species][sLearningMoveTableID] & 0x1FF);
-        sLearningMoveTableID++;
-        retVal = GiveMoveToMon(mon, gMoveToLearn);
-    }
+    if (firstMove)
+    {
+        sLearningMoveTableID = 0;
+
+        while (LEVEL_UP_LEARNSET_MOVE(gLevelUpLearnsets[species], sLearningMoveTableID) != LEVEL_UP_LEARNSET_END
+            && LEVEL_UP_LEARNSET_LEVEL(gLevelUpLearnsets[species], sLearningMoveTableID) != level)
+        {
+            sLearningMoveTableID++;
+        }
+
+        if (LEVEL_UP_LEARNSET_MOVE(gLevelUpLearnsets[species], sLearningMoveTableID) == LEVEL_UP_LEARNSET_END)
+            return 0;
+    }
+
+    if (LEVEL_UP_LEARNSET_LEVEL(gLevelUpLearnsets[species], sLearningMoveTableID) == level)
+    {
+        gMoveToLearn = LEVEL_UP_LEARNSET_MOVE(gLevelUpLearnsets[species], sLearningMoveTableID);
+        sLearningMoveTableID++;
+        retVal = GiveMoveToMon(mon, gMoveToLearn);
+    }
 
     return retVal;
 }
diff --git a/src/pokemon_3.c b/src/pokemon_3.c
--- a/src/pokemon_3.c
+++ b/src/pokemon_3.c
@@ -1006,29 +1006,31 @@
     for (i = 0; i < 4; i++)
         knownMoves[i] = GetMonData(mon, MON_DATA_MOVE1 + i, 0);
 
-    for (i = 0; i < MAX_MOVE_TUTOR_MOVES; i++)
-    {
-        u16 moveLevel;
-
-        if (gLevelUpLearnsets[species][i] == 0xFFFF)
-            break;
-
-        moveLevel = gLevelUpLearnsets[species][i] & 0xFE00;
-        if (moveLevel <= (level << 9))
-        {
-            for (j = 0; j < 4 && knownMoves[j] != (gLevelUpLearnsets[species][i] & 0x1FF); j++)
-                ;
-
-            if (j == 4)
-            {
-                for (k = 0; k < numMoves && moves[k] != (gLevelUpLearnsets[species][i] & 0x1FF); k++)
-                    ;
-
-                if (k == numMoves)
-                    moves[numMoves++] = gLevelUpLearnsets[species][i] & 0x1FF;
-            }
-        }
-    }
+    for (i = 0; i < MAX_MOVE_TUTOR_MOVES; i++)
+    {
+        u16 moveLevel;
+        RubyMoveId move;
+
+        move = LEVEL_UP_LEARNSET_MOVE(gLevelUpLearnsets[species], i);
+        if (move == LEVEL_UP_LEARNSET_END)
+            break;
+
+        moveLevel = LEVEL_UP_LEARNSET_LEVEL(gLevelUpLearnsets[species], i);
+        if (moveLevel <= level)
+        {
+            for (j = 0; j < 4 && knownMoves[j] != move; j++)
+                ;
+
+            if (j == 4)
+            {
+                for (k = 0; k < numMoves && moves[k] != move; k++)
+                    ;
+
+                if (k == numMoves)
+                    moves[numMoves++] = move;
+            }
+        }
+    }
 
     return numMoves;
 }
@@ -1038,8 +1040,8 @@
     u8 numMoves = 0;
     int i;
 
-    for (i = 0; i < 20 && gLevelUpLearnsets[species][i] != 0xFFFF; i++)
-         moves[numMoves++] = gLevelUpLearnsets[species][i] & 0x1FF;
+    for (i = 0; i < 20 && LEVEL_UP_LEARNSET_MOVE(gLevelUpLearnsets[species], i) != LEVEL_UP_LEARNSET_END; i++)
+         moves[numMoves++] = LEVEL_UP_LEARNSET_MOVE(gLevelUpLearnsets[species], i);
 
      return numMoves;
 }
@@ -1059,30 +1061,32 @@
     for (i = 0; i < 4; i++)
         learnedMoves[i] = GetMonData(mon, MON_DATA_MOVE1 + i, 0);
 
-    for (i = 0; i < 20; i++)
-    {
-        u16 moveLevel;
-
-        if (gLevelUpLearnsets[species][i] == 0xFFFF)
-            break;
-
-        moveLevel = gLevelUpLearnsets[species][i] & 0xFE00;
-
-        if (moveLevel <= (level << 9))
-        {
-            for (j = 0; j < 4 && learnedMoves[j] != (gLevelUpLearnsets[species][i] & 0x1FF); j++)
-                ;
-
-            if (j == 4)
-            {
-                for (k = 0; k < numMoves && moves[k] != (gLevelUpLearnsets[species][i] & 0x1FF); k++)
-                    ;
-
-                if (k == numMoves)
-                    moves[numMoves++] = gLevelUpLearnsets[species][i] & 0x1FF;
-            }
-        }
-    }
+    for (i = 0; i < 20; i++)
+    {
+        u16 moveLevel;
+        RubyMoveId move;
+
+        move = LEVEL_UP_LEARNSET_MOVE(gLevelUpLearnsets[species], i);
+        if (move == LEVEL_UP_LEARNSET_END)
+            break;
+
+        moveLevel = LEVEL_UP_LEARNSET_LEVEL(gLevelUpLearnsets[species], i);
+
+        if (moveLevel <= level)
+        {
+            for (j = 0; j < 4 && learnedMoves[j] != move; j++)
+                ;
+
+            if (j == 4)
+            {
+                for (k = 0; k < numMoves && moves[k] != move; k++)
+                    ;
+
+                if (k == numMoves)
+                    moves[numMoves++] = move;
+            }
+        }
+    }
 
     return numMoves;
 }
